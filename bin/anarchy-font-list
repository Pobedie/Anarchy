#!/bin/bash

# anarchy:summary=List available monospace fonts
# anarchy:examples=anarchy font list | anarchy font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|anarchy' | sort -u
