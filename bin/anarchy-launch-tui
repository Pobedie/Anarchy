#!/bin/bash

# anarchy:summary=Launch a TUI command in the default terminal with ANARCHY styling
# anarchy:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.anarchy.$(basename $1) -e "$1" "${@:2}"
