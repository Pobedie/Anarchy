#!/bin/bash

# anarchy:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/anarchy/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  ANARCHY_THEME_SKIP_BACKGROUND=1 anarchy-theme-set "$(cat $THEME_NAME_PATH)"
fi
