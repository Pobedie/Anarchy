#!/bin/bash

# anarchy:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/anarchy/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! anarchy-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
