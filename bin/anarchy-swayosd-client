#!/bin/bash

# anarchy:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# anarchy:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(anarchy-hyprland-monitor-focused)" "$@"
