#!/bin/bash

# anarchy:summary=Launch a terminal in the active terminal's current directory
# anarchy:args=[command...]

exec setsid uwsm-app -- xdg-terminal-exec --dir="$(anarchy-cmd-terminal-cwd)" "$@"
