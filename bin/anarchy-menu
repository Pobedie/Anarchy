#!/bin/bash

# anarchy:summary=Launch the ANARCHY Menu or takes a parameter to jump straight to a submenu.

# Set to true when going directly to a submenu, so we can exit directly
BACK_TO_EXIT=false

back_to() {
  local parent_menu="$1"

  if [[ $BACK_TO_EXIT == "true" ]]; then
    exit 0
  elif [[ -n $parent_menu ]]; then
    "$parent_menu"
  else
    show_main_menu
  fi
}

toggle_existing_menu() {
  if pgrep -f "walker.*--dmenu" >/dev/null; then
    walker --close >/dev/null 2>&1
    exit 0
  fi
}

menu() {
  local prompt="$1"
  local options="$2"
  local extra="$3"
  local preselect="$4"

  read -r -a args <<<"$extra"

  if [[ -n $preselect ]]; then
    local index
    index=$(echo -e "$options" | grep -nxF "$preselect" | cut -d: -f1)
    if [[ -n $index ]]; then
      args+=("-c" "$index")
    fi
  fi

  echo -e "$options" | anarchy-launch-walker --dmenu --width 295 --minheight 1 --maxheight 630 -p "$prompt…" "${args[@]}" 2>/dev/null
}

terminal() {
  xdg-terminal-exec --app-id=org.anarchy.terminal "$@"
}

present_terminal() {
  anarchy-launch-floating-terminal-with-presentation $1
}

open_in_editor() {
  notify-send -u low "Editing config file" "$1"
  anarchy-launch-editor "$1"
}

hypr_config_file() {
  local name="$1"
  printf '%s/.config/hypr/%s.lua' "$HOME" "$name"
}

install() {
  present_terminal "echo 'Installing $1...'; anarchy-pkg-add $2"
}

install_and_launch() {
  present_terminal "echo 'Installing $1...'; anarchy-pkg-add $2 && setsid gtk-launch $3"
}

install_font() {
  present_terminal "echo 'Installing $1...'; anarchy-pkg-add $2 && sleep 2 && anarchy-font-set '$3'"
}

install_terminal() {
  present_terminal "anarchy-install-terminal $1"
}

aur_install() {
  present_terminal "echo 'Installing $1 from AUR...'; anarchy-pkg-aur-add $2"
}

aur_install_and_launch() {
  present_terminal "echo 'Installing $1 from AUR...'; anarchy-pkg-aur-add $2 && setsid gtk-launch $3"
}

show_learn_menu() {
  case $(menu "Learn" "  Keybindings\n  Tmux keybindings\n  ANARCHY\n  Hyprland\n󰣇  Arch\n  Neovim\n󱆃  Bash") in
  *Tmux*) anarchy-menu-tmux-keybindings ;;
  *Keybindings*) anarchy-menu-keybindings ;;
  *ANARCHY*) anarchy-launch-webapp "https://learn.omacom.io/2/the-anarchy-manual" ;;
  *Hyprland*) anarchy-launch-webapp "https://wiki.hypr.land/" ;;
  *Arch*) anarchy-launch-webapp "https://wiki.archlinux.org/title/Main_page" ;;
  *Bash*) anarchy-launch-webapp "https://devhints.io/bash" ;;
  *Neovim*) anarchy-launch-webapp "https://www.lazyvim.org/keymaps" ;;
  *) show_main_menu ;;
  esac
}

show_trigger_menu() {
  case $(menu "Trigger" "󰔛  Reminder\n  Capture\n󰧸  Transcode\n  Share\n󰔎  Toggle\n  Hardware") in
  *Reminder*) show_reminder_menu ;;
  *Capture*) show_capture_menu ;;
  *Transcode*) anarchy-transcode || back_to show_trigger_menu ;;
  *Share*) show_share_menu ;;
  *Toggle*) show_toggle_menu ;;
  *Hardware*) show_hardware_menu ;;
  *) show_main_menu ;;
  esac
}

show_reminder_menu() {
  case $(menu "Reminder" "󰔛  Set one\n󰔛  Show all\n󰔛  Clear all") in
  *Set*) show_custom_reminder_input ;;
  *"Show all"*) anarchy-reminder show ;;
  *"Clear all"*) anarchy-reminder clear ;;
  *) back_to show_trigger_menu ;;
  esac
}

show_custom_reminder_input() {
  local minutes
  minutes=$(anarchy-menu-input "Remind in minutes")

  if [[ $minutes =~ ^[0-9]+$ ]] && ((minutes > 0)); then
    show_reminder_message_input "$minutes"
  elif [[ -n $minutes ]]; then
    anarchy-notification-send -g 󰔛 "Invalid reminder" "Enter the number of minutes" -u critical
    show_custom_reminder_input
  else
    back_to show_reminder_menu
  fi
}

show_reminder_message_input() {
  local minutes="$1"
  local message
  message=$(anarchy-menu-input "Reminder message")

  if [[ -n $message ]]; then
    anarchy-reminder "$minutes" "$message"
  else
    anarchy-reminder "$minutes"
  fi
}

show_capture_menu() {
  case $(menu "Capture" "  Screenshot\n  Screenrecord\n󰴑  Text Extraction\n󰃉  Color") in
  *Screenshot*) anarchy-capture-screenshot ;;
  *Screenrecord*) show_screenrecord_menu ;;
  *Text*) anarchy-capture-text-extraction ;;
  *Color*) pkill hyprpicker || hyprpicker -a ;;
  *) back_to show_trigger_menu ;;
  esac
}

get_webcam_list() {
  v4l2-ctl --list-devices 2>/dev/null | while IFS= read -r line; do
    if [[ $line != $'\t'* && -n $line ]]; then
      local name="$line"
      IFS= read -r device || break
      device=$(echo "$device" | tr -d '\t' | head -1)
      [[ -n $device ]] && echo "$device  $name"
    fi
  done
}

show_webcam_select_menu() {
  local devices=$(get_webcam_list)
  local count=$(echo "$devices" | grep -c . 2>/dev/null || echo 0)

  if [[ -z $devices ]] || ((count == 0)); then
    notify-send "No webcam devices found" -u critical -t 3000
    return 1
  fi

  if ((count == 1)); then
    echo "$devices" | awk '{print $1}'
  else
    menu "Select Webcam" "$devices" | awk '{print $1}'
  fi
}

show_screenrecord_menu() {
  anarchy-capture-screenrecording --stop-recording && exit 0

  case $(menu "Screenrecord" "  With no audio\n  With desktop audio\n  With desktop + microphone audio\n  With desktop + microphone audio + webcam") in
  *"With no audio") anarchy-capture-screenrecording ;;
  *"With desktop audio") anarchy-capture-screenrecording --with-desktop-audio ;;
  *"With desktop + microphone audio") anarchy-capture-screenrecording --with-desktop-audio --with-microphone-audio ;;
  *"With desktop + microphone audio + webcam")
    local device=$(show_webcam_select_menu) || {
      back_to show_capture_menu
      return
    }
    anarchy-capture-screenrecording --with-desktop-audio --with-microphone-audio --with-webcam --webcam-device="$device"
    ;;
  *) back_to show_capture_menu ;;
  esac
}

show_share_menu() {
  case $(menu "Share" "  Clipboard\n  File \n  Folder\n󰥦  Receive") in
  *Receive*) uwsm-app -- localsend ;;
  *Clipboard*) anarchy-menu-share clipboard ;;
  *File*) terminal bash -c "anarchy-menu-share file" ;;
  *Folder*) terminal bash -c "anarchy-menu-share folder" ;;
  *) back_to show_trigger_menu ;;
  esac
}

show_toggle_menu() {
  local options="󱄄  Screensaver\n󰔎  Nightlight\n󱫖  Idle Lock\n󰂛  Notifications\n󰍜  Top Bar\n󱂬  Workspace Layout\n  Window Gaps\n  1-Window Ratio\n󰍹  Monitor Scaling\n  Direct Boot\n󰟵  Passwordless Sudo"

  case $(menu "Toggle" "$options") in
  *Screensaver*) anarchy-toggle-screensaver ;;
  *Nightlight*) anarchy-toggle-nightlight ;;
  *Idle*) anarchy-toggle-idle ;;
  *Notifications*) anarchy-toggle-notification-silencing ;;
  *Bar*) anarchy-toggle-waybar ;;
  *Layout*) anarchy-hyprland-workspace-layout-toggle ;;
  *Ratio*) anarchy-hyprland-window-single-square-aspect-toggle ;;
  *Gaps*) anarchy-hyprland-window-gaps-toggle ;;
  *Scaling*) anarchy-hyprland-monitor-scaling-cycle ;;
  *"Direct Boot"*) present_terminal anarchy-config-direct-boot ;;
  *"Passwordless Sudo"*) present_terminal anarchy-sudo-passwordless ;;
  *) back_to show_trigger_menu ;;
  esac
}

show_hardware_menu() {
  local options="󰛧  Laptop Display\n 󰍹  Mirror Display"

  if anarchy-hw-hybrid-gpu; then
    options="$options\n  Hybrid GPU"
  fi

  if anarchy-hw-touchpad; then
    options="$options\n󰟸  Touchpad"
  fi

  if anarchy-hw-dell-xps-haptic-touchpad && anarchy-cmd-present dell-xps-touchpad-haptics; then
    options="$options\n󰌌  Touchpad Haptics"
  fi

  if anarchy-hw-touchscreen; then
    options="$options\n󰆽  Touchscreen"
  fi

  case $(menu "Toggle" "$options") in
  *Laptop*) anarchy-hyprland-monitor-internal toggle ;;
  *Mirror*) anarchy-hyprland-monitor-internal-mirror toggle ;;
  *Haptics*) show_hardware_touchpad_haptics_menu ;;
  *Touchpad*) anarchy-toggle-touchpad ;;
  *Touchscreen*) anarchy-toggle-touchscreen ;;
  *"Hybrid GPU"*) present_terminal anarchy-toggle-hybrid-gpu ;;
  *) back_to show_trigger_menu ;;
  esac
}

show_hardware_touchpad_haptics_menu() {
  local current=$(dell-xps-touchpad-haptics get)
  local selected=$(menu "Touchpad Haptics" "low\nmid\nhigh" "" "$current")

  if [[ -n $selected ]]; then
    dell-xps-touchpad-haptics set "$selected"
  else
    back_to show_hardware_menu
  fi
}

show_style_menu() {
  case $(menu "Style" "󰸌  Theme\n󰟵  Unlock\n  Font\n  Background\n󰍜  Waybar\n󰘇  Corners\n  Hyprland\n󱄄  Screensaver\n  About") in

  *Theme*) show_theme_menu ;;
  *Unlock*) anarchy-launch-walker -m menus:anarchyunlocks --width 800 --minheight 400 ;;
  *Font*) show_font_menu ;;
  *Background*) show_background_menu ;;
  *Corners*) show_style_corners_menu ;;
  *Hyprland*) open_in_editor "$(hypr_config_file looknfeel)" ;;
  *Waybar*) show_waybar_position_menu ;;
  *Screensaver*) show_screensaver_menu ;;
  *About*) show_about_menu ;;
  *) show_main_menu ;;
  esac
}

show_style_corners_menu() {
  case $(anarchy-menu-select "Corners" "󰝣  Sharp" "󰘇  Round") in
  *Sharp*) anarchy-style-corners sharp ;;
  *Round*) anarchy-style-corners round ;;
  *) show_style_menu ;;
  esac
}

show_waybar_position_menu() {
  case $(menu "Waybar Position" "󰁝  Top\n󰁅  Bottom\n󰁍  Left\n󰁔  Right") in
  *Top*) anarchy-style-waybar-position top ;;
  *Bottom*) anarchy-style-waybar-position bottom ;;
  *Left*) anarchy-style-waybar-position left ;;
  *Right*) anarchy-style-waybar-position right ;;
  *) show_style_menu ;;
  esac
}

show_about_menu() {
  case $(menu "About" "  Edit Text\n  Set From Image\n  Restore Default") in
  *Text*) anarchy-branding-about text ;;
  *Image*) anarchy-branding-about image ;;
  *Default*) anarchy-branding-about reset ;;
  *) show_style_menu ;;
  esac
}

show_screensaver_menu() {
  case $(menu "Screensaver" "  Edit Text\n  Set From Image\n  Restore Default") in
  *Text*) anarchy-branding-screensaver text ;;
  *Image*) anarchy-branding-screensaver image ;;
  *Default*) anarchy-branding-screensaver reset ;;
  *) show_style_menu ;;
  esac
}

show_theme_menu() {
  theme=$(anarchy-theme-switcher)
  [[ -n $theme ]] && anarchy-theme-set "$theme"
}

show_background_menu() {
  background=$(anarchy-theme-bg-switcher)
  [[ -n $background ]] && anarchy-theme-bg-set "$background"
}

show_font_menu() {
  theme=$(menu "Font" "$(anarchy-font-list)" "--width 350" "$(anarchy-font-current)")
  if [[ $theme == "CNCLD" || -z $theme ]]; then
    back_to show_style_menu
  else
    anarchy-font-set "$theme"
  fi
}

show_setup_menu() {
  local options="  Audio\n  Wifi\n󰂯  Bluetooth\n󱐋  Power Profile\n  System Sleep\n󰍹  Monitors"
  [[ -f ~/.config/hypr/bindings.lua ]] && options="$options\n  Keybindings"
  [[ -f ~/.config/hypr/input.lua ]] && options="$options\n  Input"
  options="$options\n  Defaults\n󰱔  DNS\n  Security\n  Config"

  case $(menu "Setup" "$options") in
  *Audio*) anarchy-launch-audio ;;
  *Wifi*) anarchy-launch-wifi ;;
  *Bluetooth*) anarchy-launch-bluetooth ;;
  *Power*) show_setup_power_menu ;;
  *System*) show_setup_system_menu ;;
  *Monitors*) open_in_editor "$(hypr_config_file monitors)" ;;
  *Keybindings*) open_in_editor "$(hypr_config_file bindings)" ;;
  *Input*) open_in_editor "$(hypr_config_file input)" ;;
  *Defaults*) show_setup_default_menu ;;
  *DNS*) present_terminal anarchy-setup-dns ;;
  *Security*) show_setup_security_menu ;;
  *Config*) show_setup_config_menu ;;
  *) show_main_menu ;;
  esac
}

show_setup_power_menu() {
  profile=$(menu "Power Profile" "$(anarchy-powerprofiles-list)" "" "$(powerprofilesctl get)")

  if [[ $profile == "CNCLD" || -z $profile ]]; then
    back_to show_setup_menu
  else
    powerprofilesctl set "$profile"
  fi
}

show_setup_security_menu() {
  case $(menu "Setup" "󰈷  Fingerprint\n  Fido2") in
  *Fingerprint*) present_terminal anarchy-setup-security-fingerprint ;;
  *Fido2*) present_terminal anarchy-setup-security-fido2 ;;
  *) show_setup_menu ;;
  esac
}

show_setup_default_menu() {
  case $(menu "Default" "  Browser\n  Terminal\n  Editor") in
  *Browser*) show_setup_default_browser_menu ;;
  *Terminal*) show_setup_default_terminal_menu ;;
  *Editor*) show_setup_default_editor_menu ;;
  *) show_setup_menu ;;
  esac
}

browser_desktop_exists() {
  [[ -f ~/.local/share/applications/$1 || -f ~/.nix-profile/share/applications/$1 || -f /usr/share/applications/$1 ]]
}

show_setup_default_browser_menu() {
  local options=""
  browser_desktop_exists chromium.desktop && options="$options  Chromium"
  browser_desktop_exists google-chrome.desktop && options="${options:+$options\n}󰊯  Chrome"
  browser_desktop_exists brave-browser.desktop && options="${options:+$options\n}󰖟  Brave"
  browser_desktop_exists brave-origin-beta.desktop && options="${options:+$options\n}󰖟  Brave Origin"
  browser_desktop_exists microsoft-edge.desktop && options="${options:+$options\n}󰇩  Edge"
  browser_desktop_exists firefox.desktop && options="${options:+$options\n}󰈹  Firefox"
  browser_desktop_exists zen.desktop && options="${options:+$options\n}󰖟  Zen"

  local current=""
  case "$(anarchy-default-browser)" in
  chromium) current="  Chromium" ;;
  chrome) current="󰊯  Chrome" ;;
  brave) current="󰖟  Brave" ;;
  brave-origin) current="󰖟  Brave Origin" ;;
  edge) current="󰇩  Edge" ;;
  firefox) current="󰈹  Firefox" ;;
  zen) current="󰖟  Zen" ;;
  esac

  case $(menu "Default Browser" "$options" "" "$current") in
  *Chromium*) anarchy-default-browser chromium ;;
  *Chrome*) anarchy-default-browser chrome ;;
  *"Brave Origin"*) anarchy-default-browser brave-origin ;;
  *Brave*) anarchy-default-browser brave ;;
  *Edge*) anarchy-default-browser edge ;;
  *Firefox*) anarchy-default-browser firefox ;;
  *Zen*) anarchy-default-browser zen ;;
  *) show_setup_default_menu ;;
  esac
}

show_setup_default_terminal_menu() {
  local options=""
  anarchy-cmd-present alacritty && options="$options  Alacritty"
  anarchy-cmd-present foot && options="${options:+$options\n}  Foot"
  anarchy-cmd-present ghostty && options="${options:+$options\n}  Ghostty"
  anarchy-cmd-present kitty && options="${options:+$options\n}  Kitty"

  local current=""
  case "$(anarchy-default-terminal)" in
  alacritty) current="  Alacritty" ;;
  foot) current="  Foot" ;;
  ghostty) current="  Ghostty" ;;
  kitty) current="  Kitty" ;;
  esac

  case $(menu "Default Terminal" "$options" "" "$current") in
  *Alacritty*) anarchy-default-terminal alacritty ;;
  *Foot*) anarchy-default-terminal foot ;;
  *Ghostty*) anarchy-default-terminal ghostty ;;
  *Kitty*) anarchy-default-terminal kitty ;;
  *) show_setup_default_menu ;;
  esac
}

show_setup_default_editor_menu() {
  local options=""
  anarchy-cmd-present nvim && options="$options  Neovim"
  anarchy-cmd-present code && options="${options:+$options\n}  VSCode"
  anarchy-cmd-present cursor && options="${options:+$options\n}  Cursor"
  anarchy-cmd-present zeditor && options="${options:+$options\n}  Zed"
  anarchy-cmd-present sublime_text && options="${options:+$options\n}  Sublime Text"
  anarchy-cmd-present helix && options="${options:+$options\n}  Helix"
  anarchy-cmd-present vim && options="${options:+$options\n}  Vim"
  anarchy-cmd-present emacs && options="${options:+$options\n}  Emacs"

  local current=""
  case "$(anarchy-default-editor)" in
  nvim) current="  Neovim" ;;
  code) current="  VSCode" ;;
  cursor) current="  Cursor" ;;
  zed | zeditor) current="  Zed" ;;
  sublime_text) current="  Sublime Text" ;;
  helix) current="  Helix" ;;
  vim) current="  Vim" ;;
  emacs) current="  Emacs" ;;
  esac

  case $(menu "Default Editor" "$options" "" "$current") in
  *Neovim*) anarchy-default-editor nvim ;;
  *VSCode*) anarchy-default-editor code ;;
  *Cursor*) anarchy-default-editor cursor ;;
  *Zed*) anarchy-default-editor zed ;;
  *Sublime*) anarchy-default-editor sublime_text ;;
  *Helix*) anarchy-default-editor helix ;;
  *Vim*) anarchy-default-editor vim ;;
  *Emacs*) anarchy-default-editor emacs ;;
  *) show_setup_default_menu ;;
  esac
}

show_setup_config_menu() {
  case $(menu "Setup" "  Hyprland\n  Hypridle\n  Hyprlock\n  Hyprsunset\n  Swayosd\n󰌧  Walker\n󰍜  Waybar\n󰞅  XCompose") in
  *Hyprland*) open_in_editor "$(hypr_config_file hyprland)" ;;
  *Hypridle*) open_in_editor ~/.config/hypr/hypridle.conf && anarchy-restart-hypridle ;;
  *Hyprlock*) open_in_editor ~/.config/hypr/hyprlock.conf ;;
  *Hyprsunset*) open_in_editor ~/.config/hypr/hyprsunset.conf && anarchy-restart-hyprsunset ;;
  *Swayosd*) open_in_editor ~/.config/swayosd/config.toml && anarchy-restart-swayosd ;;
  *Walker*) open_in_editor ~/.config/walker/config.toml && anarchy-restart-walker ;;
  *Waybar*) open_in_editor ~/.config/waybar/config.jsonc && anarchy-restart-waybar ;;
  *) show_setup_menu ;;
  esac
}

show_setup_system_menu() {
  local options=""

  if anarchy-toggle-enabled suspend-off; then
    options="$options󰒲  Enable Suspend"
  else
    options="$options󰒲  Disable Suspend"
  fi

  if anarchy-hibernation-available; then
    options="$options\n󰤁  Disable Hibernate"
  else
    options="$options\n󰤁  Enable Hibernate"
  fi

  case $(menu "System" "$options") in
  *Suspend*) anarchy-toggle-suspend ;;
  *"Enable Hibernate"*) present_terminal anarchy-hibernation-setup ;;
  *"Disable Hibernate"*) present_terminal anarchy-hibernation-remove ;;
  *) show_setup_menu ;;
  esac
}

show_install_menu() {
  case $(menu "Install" "󰣇  Package\n󰣇  AUR\n  Web App\n  TUI\n  Service\n  Style\n󰵮  Development\n  Editor\n  Terminal\n  Browser\n󱚤  AI\n  Gaming\n󰍲  Windows") in
  *Package*) terminal anarchy-pkg-install ;;
  *AUR*) terminal anarchy-pkg-aur-install ;;
  *Web*) present_terminal anarchy-webapp-install ;;
  *TUI*) present_terminal anarchy-tui-install ;;
  *Service*) show_install_service_menu ;;
  *Style*) show_install_style_menu ;;
  *Development*) show_install_development_menu ;;
  *Editor*) show_install_editor_menu ;;
  *Terminal*) show_install_terminal_menu ;;
  *Browser*) show_install_browser_menu ;;
  *Gaming*) show_install_gaming_menu ;;
  *AI*) show_install_ai_menu ;;
  *Windows*) present_terminal "anarchy-windows-vm install" ;;
  *) show_main_menu ;;
  esac
}

show_install_browser_menu() {
  case $(menu "Install" "  Chrome\n  Edge\n  Brave\n  Brave Origin\n  Firefox\n󰖟  Zen") in
  *Chrome*) present_terminal "anarchy-install-browser chrome" ;;
  *Edge*) present_terminal "anarchy-install-browser edge" ;;
  *"Brave Origin"*) present_terminal "anarchy-install-browser brave-origin" ;;
  *Brave*) present_terminal "anarchy-install-browser brave" ;;
  *Firefox*) present_terminal "anarchy-install-browser firefox" ;;
  *Zen*) present_terminal "anarchy-install-browser zen" ;;
  *) show_install_menu ;;
  esac
}

show_install_service_menu() {
  case $(menu "Install" "☀  Sunshine\n󰟵  Bitwarden\n  Chromium Account") in
  *Sunshine*) present_terminal anarchy-install-service-sunshine ;;
  *Bitwarden*) install_and_launch "Bitwarden" "bitwarden bitwarden-cli" "bitwarden" ;;
  *Chromium*) present_terminal anarchy-install-chromium-google-account ;;
  *) show_install_menu ;;
  esac
}

show_install_editor_menu() {
  case $(menu "Install" "  VSCode\n  Cursor\n  Zed\n  Sublime Text\n  Helix\n  Vim\n  Emacs") in
  *VSCode*) present_terminal anarchy-install-vscode ;;
  *Cursor*) install_and_launch "Cursor" "cursor-bin" "cursor" ;;
  *Zed*) present_terminal anarchy-install-zed ;;
  *Sublime*) install_and_launch "Sublime Text" "sublime-text-4" "sublime_text" ;;
  *Helix*) present_terminal anarchy-install-helix ;;
  *Vim*) install "Vim" "vim" ;;
  *Emacs*) install "Emacs" "emacs-wayland" && systemctl --user enable --now emacs.service ;;
  *) show_install_menu ;;
  esac
}

show_install_terminal_menu() {
  case $(menu "Install" "  Alacritty\n  Foot\n  Ghostty\n  Kitty") in
  *Alacritty*) install_terminal "alacritty" ;;
  *Foot*) install_terminal "foot" ;;
  *Ghostty*) install_terminal "ghostty" ;;
  *Kitty*) install_terminal "kitty" ;;
  *) show_install_menu ;;
  esac
}

show_install_ai_menu() {
  ollama_pkg=$(
    (anarchy-cmd-present nvidia-smi && echo ollama-cuda) ||
      (anarchy-cmd-present rocminfo && echo ollama-rocm) ||
      echo ollama
  )

  case $(menu "Install" "  Dictation\n󱚤  LM Studio\n󱚤  Ollama\n󱚤  Crush") in
  *Dictation*) present_terminal anarchy-voxtype-install ;;
  *Studio*) install "LM Studio" "lmstudio-bin" ;;
  *Ollama*) install "Ollama" $ollama_pkg ;;
  *Crush*) install "Crush" "crush-bin" ;;
  *) show_install_menu ;;
  esac
}

show_install_gaming_menu() {
  case $(menu "Install" "  Steam\n󰯉  RetroArch\n󰍳  Minecraft\n󰢹  NVIDIA GeForce NOW\n  Xbox Cloud Gaming\n󰂯  Xbox Controller\n󰍹  Moonlight (GameStream)\n  Lutris (Battle.net)\n󱓟  Heroic (Epic Games)\n󰯉  RetroArch Game Launcher") in
  *Steam*) present_terminal anarchy-install-gaming-steam ;;
  *"RetroArch Game Launcher"*) anarchy-games-retro-install ;;
  *RetroArch*) present_terminal anarchy-install-gaming-retroarch ;;
  *Minecraft*) install_and_launch "Minecraft" "minecraft-launcher" "minecraft-launcher" ;;
  *GeForce*) present_terminal anarchy-install-gaming-geforce-now ;;
  *"Xbox Cloud"*) present_terminal anarchy-install-gaming-xbox-cloud ;;
  *Xbox*) present_terminal anarchy-install-gaming-xbox-controllers ;;
  *Lutris*) present_terminal anarchy-install-gaming-lutris ;;
  *Heroic*) present_terminal anarchy-install-gaming-heroic ;;
  *Moonlight*) present_terminal anarchy-install-gaming-moonlight ;;
  *) show_install_menu ;;
  esac
}

show_install_style_menu() {
  case $(menu "Install" "󰸌  Theme\n  Background\n  Font") in
  *Theme*) present_terminal anarchy-theme-install ;;
  *Background*) anarchy-theme-bg-install ;;
  *Font*) show_install_font_menu ;;
  *) show_install_menu ;;
  esac
}

show_install_font_menu() {
  case $(menu "Install" "  Cascadia Mono\n  Meslo LG Mono\n  Fira Code\n  Victor Code\n  Bitstream Vera Mono\n  Iosevka" "--width 350") in
  *Cascadia*) install_font "Cascadia Mono" "ttf-cascadia-mono-nerd" "CaskaydiaMono Nerd Font" ;;
  *Meslo*) install_font "Meslo LG Mono" "ttf-meslo-nerd" "MesloLGL Nerd Font" ;;
  *Fira*) install_font "Fira Code" "ttf-firacode-nerd" "FiraCode Nerd Font" ;;
  *Victor*) install_font "Victor Code" "ttf-victor-mono-nerd" "VictorMono Nerd Font" ;;
  *Bitstream*) install_font "Bitstream Vera Code" "ttf-bitstream-vera-mono-nerd" "BitstromWera Nerd Font" ;;
  *Iosevka*) install_font "Iosevka" "ttf-iosevka-nerd" "Iosevka Nerd Font Mono" ;;
  *) show_install_menu ;;
  esac
}

show_install_development_menu() {
  case $(menu "Install" "󰫏  Ruby on Rails\n  Docker DB\n  JavaScript\n  Go\n  PHP\n  Python\n  Elixir\n  Zig\n  Rust\n  Java\n  .NET\n  OCaml\n  Clojure\n  Scala") in
  *Rails*) present_terminal "anarchy-install-dev-env ruby" ;;
  *Docker*) present_terminal anarchy-install-docker-dbs ;;
  *JavaScript*) show_install_javascript_menu ;;
  *Go*) present_terminal "anarchy-install-dev-env go" ;;
  *PHP*) show_install_php_menu ;;
  *Python*) present_terminal "anarchy-install-dev-env python" ;;
  *Elixir*) show_install_elixir_menu ;;
  *Zig*) present_terminal "anarchy-install-dev-env zig" ;;
  *Rust*) present_terminal "anarchy-install-dev-env rust" ;;
  *Java*) present_terminal "anarchy-install-dev-env java" ;;
  *NET*) present_terminal "anarchy-install-dev-env dotnet" ;;
  *OCaml*) present_terminal "anarchy-install-dev-env ocaml" ;;
  *Clojure*) present_terminal "anarchy-install-dev-env clojure" ;;
  *Scala*) present_terminal "anarchy-install-dev-env scala" ;;
  *) show_install_menu ;;
  esac
}

show_install_javascript_menu() {
  case $(menu "Install" "  Node.js\n  Bun\n  Deno") in
  *Node*) present_terminal "anarchy-install-dev-env node" ;;
  *Bun*) present_terminal "anarchy-install-dev-env bun" ;;
  *Deno*) present_terminal "anarchy-install-dev-env deno" ;;
  *) show_install_development_menu ;;
  esac
}

show_install_php_menu() {
  case $(menu "Install" "  PHP\n  Laravel\n  Symfony") in
  *PHP*) present_terminal "anarchy-install-dev-env php" ;;
  *Laravel*) present_terminal "anarchy-install-dev-env laravel" ;;
  *Symfony*) present_terminal "anarchy-install-dev-env symfony" ;;
  *) show_install_development_menu ;;
  esac
}

show_install_elixir_menu() {
  case $(menu "Install" "  Elixir\n  Phoenix") in
  *Elixir*) present_terminal "anarchy-install-dev-env elixir" ;;
  *Phoenix*) present_terminal "anarchy-install-dev-env phoenix" ;;
  *) show_install_development_menu ;;
  esac
}

show_remove_menu() {
  case $(menu "Remove" "󰣇  Package\n  Web App\n  TUI\n󰵮  Development\n󰸌  Theme\n  Browser\n  Dictation\n  Gaming\n󰍲  Windows\n󰏓  Preinstalls\n  Security") in
  *Package*) terminal anarchy-pkg-remove ;;
  *Web*) present_terminal anarchy-webapp-remove ;;
  *TUI*) present_terminal anarchy-tui-remove ;;
  *Development*) show_remove_development_menu ;;
  *Theme*) present_terminal anarchy-theme-remove ;;
  *Browser*) show_remove_browser_menu ;;
  *Dictation*) present_terminal anarchy-voxtype-remove ;;
  *Gaming*) show_remove_gaming_menu ;;
  *Windows*) present_terminal "anarchy-windows-vm remove" ;;
  *Preinstalls*) present_terminal anarchy-remove-preinstalls ;;
  *Security*) show_remove_security_menu ;;
  *) show_main_menu ;;
  esac
}

show_remove_security_menu() {
  case $(menu "Remove" "󰈷  Fingerprint\n  Fido2") in
  *Fingerprint*) present_terminal anarchy-remove-security-fingerprint ;;
  *Fido2*) present_terminal anarchy-remove-security-fido2 ;;
  *) show_remove_menu ;;
  esac
}

show_remove_browser_menu() {
  case $(menu "Remove" "  Chrome\n  Edge\n  Brave\n  Brave Origin\n  Firefox\n  Zen") in
  *Chrome*) present_terminal "anarchy-remove-browser chrome" ;;
  *Edge*) present_terminal "anarchy-remove-browser edge" ;;
  *"Brave Origin"*) present_terminal "anarchy-remove-browser brave-origin" ;;
  *Brave*) present_terminal "anarchy-remove-browser brave" ;;
  *Firefox*) present_terminal "anarchy-remove-browser firefox" ;;
  *Zen*) present_terminal "anarchy-remove-browser zen" ;;
  *) show_remove_menu ;;
  esac
}

show_remove_gaming_menu() {
  case $(menu "Remove" "  Steam\n  RetroArch\n󰍳  Minecraft\n󰢹  NVIDIA GeForce NOW\n  Xbox Cloud Gaming\n󰖺  Xbox Controller (󰂯)\n󰍹  Moonlight (GameStream)\n  Lutris (Battle.net)\n󱓟  Heroic (Epic Games)") in
  *Steam*) present_terminal anarchy-remove-gaming-steam ;;
  *RetroArch*) present_terminal anarchy-remove-gaming-retroarch ;;
  *Minecraft*) present_terminal anarchy-remove-gaming-minecraft ;;
  *GeForce*) present_terminal anarchy-remove-gaming-geforce-now ;;
  *"Xbox Cloud"*) present_terminal anarchy-remove-gaming-xbox-cloud ;;
  *Xbox*) present_terminal anarchy-remove-gaming-xbox-controllers ;;
  *Moonlight*) present_terminal anarchy-remove-gaming-moonlight ;;
  *Lutris*) present_terminal anarchy-remove-gaming-lutris ;;
  *Heroic*) present_terminal anarchy-remove-gaming-heroic ;;
  *) show_remove_menu ;;
  esac
}

show_remove_development_menu() {
  case $(menu "Remove" "󰫏  Ruby on Rails\n  JavaScript\n  Go\n  PHP\n  Python\n  Elixir\n  Zig\n  Rust\n  Java\n  .NET\n  OCaml\n  Clojure\n  Scala") in
  *Rails*) present_terminal "anarchy-remove-dev-env ruby" ;;
  *JavaScript*) show_remove_javascript_menu ;;
  *Go*) present_terminal "anarchy-remove-dev-env go" ;;
  *PHP*) show_remove_php_menu ;;
  *Python*) present_terminal "anarchy-remove-dev-env python" ;;
  *Elixir*) show_remove_elixir_menu ;;
  *Zig*) present_terminal "anarchy-remove-dev-env zig" ;;
  *Rust*) present_terminal "anarchy-remove-dev-env rust" ;;
  *Java*) present_terminal "anarchy-remove-dev-env java" ;;
  *NET*) present_terminal "anarchy-remove-dev-env dotnet" ;;
  *OCaml*) present_terminal "anarchy-remove-dev-env ocaml" ;;
  *Clojure*) present_terminal "anarchy-remove-dev-env clojure" ;;
  *Scala*) present_terminal "anarchy-remove-dev-env scala" ;;
  *) show_remove_menu ;;
  esac
}

show_remove_javascript_menu() {
  case $(menu "Remove" "  Node.js\n  Bun\n  Deno") in
  *Node*) present_terminal "anarchy-remove-dev-env node" ;;
  *Bun*) present_terminal "anarchy-remove-dev-env bun" ;;
  *Deno*) present_terminal "anarchy-remove-dev-env deno" ;;
  *) show_remove_development_menu ;;
  esac
}

show_remove_php_menu() {
  case $(menu "Remove" "  PHP\n  Laravel\n  Symfony") in
  *PHP*) present_terminal "anarchy-remove-dev-env php" ;;
  *Laravel*) present_terminal "anarchy-remove-dev-env laravel" ;;
  *Symfony*) present_terminal "anarchy-remove-dev-env symfony" ;;
  *) show_remove_development_menu ;;
  esac
}

show_remove_elixir_menu() {
  case $(menu "Remove" "  Elixir\n  Phoenix") in
  *Elixir*) present_terminal "anarchy-remove-dev-env elixir" ;;
  *Phoenix*) present_terminal "anarchy-remove-dev-env phoenix" ;;
  *) show_remove_development_menu ;;
  esac
}

show_update_menu() {
  case $(menu "Update" "  ANARCHY\n󰔫  Channel\n  Config\n󰸌  Extra Themes\n  Process\n󰇅  Hardware\n  Firmware\n  Password\n  Timezone\n  Time") in
  *ANARCHY*) present_terminal anarchy-update ;;
  *Channel*) show_update_channel_menu ;;
  *Config*) show_update_config_menu ;;
  *Themes*) present_terminal anarchy-theme-update ;;
  *Process*) show_update_process_menu ;;
  *Hardware*) show_update_hardware_menu ;;
  *Firmware*) present_terminal anarchy-update-firmware ;;
  *Timezone*) present_terminal anarchy-tz-select ;;
  *Time*) present_terminal anarchy-update-time ;;
  *Password*) show_update_password_menu ;;
  *) show_main_menu ;;
  esac
}

show_update_channel_menu() {
  case $(menu "Update channel" "🟢 Stable\n🟡 RC\n🟠 Edge\n🔴 Dev") in
  *Stable*) present_terminal "anarchy-channel-set stable" ;;
  *RC*) present_terminal "anarchy-channel-set rc" ;;
  *Edge*) present_terminal "anarchy-channel-set edge" ;;
  *Dev*) present_terminal "anarchy-channel-set dev" ;;
  *) show_update_menu ;;
  esac
}
show_update_process_menu() {
  case $(menu "Restart" "  Hypridle\n  Hyprsunset\n󰎟  Mako\n  Swayosd\n󰌧  Walker\n󰍜  Waybar") in
  *Hypridle*) anarchy-restart-hypridle ;;
  *Hyprsunset*) anarchy-restart-hyprsunset ;;
  *Mako*) anarchy-restart-mako ;;
  *Swayosd*) anarchy-restart-swayosd ;;
  *Walker*) anarchy-restart-walker ;;
  *Waybar*) anarchy-restart-waybar ;;
  *) show_update_menu ;;
  esac
}

show_update_config_menu() {
  case $(menu "Use default config" "  Hyprland\n  Hypridle\n  Hyprlock\n  Hyprsunset\n󱣴  Plymouth\n  Swayosd\n  Tmux\n󰌧  Walker\n󰍜  Waybar") in
  *Hyprland*) present_terminal anarchy-refresh-hyprland ;;
  *Hypridle*) present_terminal anarchy-refresh-hypridle ;;
  *Hyprlock*) present_terminal anarchy-refresh-hyprlock ;;
  *Hyprsunset*) present_terminal anarchy-refresh-hyprsunset ;;
  *Plymouth*) present_terminal anarchy-refresh-plymouth ;;
  *Swayosd*) present_terminal anarchy-refresh-swayosd ;;
  *Tmux*) present_terminal anarchy-refresh-tmux ;;
  *Walker*) present_terminal anarchy-refresh-walker ;;
  *Waybar*) present_terminal anarchy-refresh-waybar ;;
  *) show_update_menu ;;
  esac
}

show_update_hardware_menu() {
  case $(menu "Restart" "  Audio\n󱚾  Wi-Fi\n󰂯  Bluetooth\n󰟸  Trackpad") in
  *Audio*) present_terminal anarchy-restart-pipewire ;;
  *Wi-Fi*) present_terminal anarchy-restart-wifi ;;
  *Bluetooth*) present_terminal anarchy-restart-bluetooth ;;
  *Trackpad*) present_terminal anarchy-restart-trackpad ;;
  *) show_update_menu ;;
  esac
}

show_update_password_menu() {
  case $(menu "Update Password" "  Drive Encryption\n  User") in
  *Drive*) present_terminal anarchy-drive-password ;;
  *User*) present_terminal passwd ;;
  *) show_update_menu ;;
  esac
}

show_about() {
  anarchy-launch-about
}

show_system_menu() {
  local options="󱄄  Screensaver\n  Lock"
  ! anarchy-toggle-enabled suspend-off && options="$options\n󰒲  Suspend"
  anarchy-hibernation-available && options="$options\n󰤁  Hibernate"
  options="$options\n󰍃  Logout\n󰜉  Restart\n󰐥  Shutdown"

  case $(menu "System" "$options") in
  *Screensaver*) anarchy-launch-screensaver force ;;
  *Lock*) anarchy-system-lock ;;
  *Suspend*) systemctl suspend ;;
  *Hibernate*) systemctl hibernate ;;
  *Logout*) anarchy-system-logout ;;
  *Restart*) anarchy-system-reboot ;;
  *Shutdown*) anarchy-system-shutdown ;;
  *) back_to show_main_menu ;;
  esac
}

show_main_menu() {
  go_to_menu "$(menu "Go" "󰀻  Apps\n󰧑  Learn\n󱓞  Trigger\n  Style\n  Setup\n󰉉  Install\n󰭌  Remove\n  Update\n  About\n  System")"
}

go_to_menu() {
  case "${1,,}" in
  *apps*) walker -p "Launch…" ;;
  *learn*) show_learn_menu ;;
  *trigger*) show_trigger_menu ;;
  *toggle*) show_toggle_menu ;;
  *hardware*) show_hardware_menu ;;
  *share*) show_share_menu ;;
  *reminder-set*) show_custom_reminder_input ;;
  *reminder*) show_reminder_menu ;;
  *background*) show_background_menu ;;
  *capture*) show_capture_menu ;;
  *style*) show_style_menu ;;
  *theme*) show_theme_menu ;;
  *screenrecord*) show_screenrecord_menu ;;
  *setup*) show_setup_menu ;;
  *power*) show_setup_power_menu ;;
  *install*) show_install_menu ;;
  *remove*) show_remove_menu ;;
  *update*) show_update_menu ;;
  *about*) show_about ;;
  *system*) show_system_menu ;;
  esac
}

# Allow user extensions and overrides
USER_EXTENSIONS="$HOME/.config/anarchy/extensions/menu.sh"
[[ -f $USER_EXTENSIONS ]] && source "$USER_EXTENSIONS"

toggle_existing_menu

if [[ -n $1 ]]; then
  BACK_TO_EXIT=true
  go_to_menu "$1"
else
  show_main_menu
fi
